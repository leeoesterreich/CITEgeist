# CITEgeist runtime dependencies (CPU-installable subset)
# Core scientific computing
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0
matplotlib>=3.5.0
seaborn>=0.11.0

# Single-cell and spatial analysis
scanpy>=1.9.0
squidpy>=1.2.0
anndata>=0.8.0
scikit-learn>=1.0.0

# Spatial statistics
esda>=2.4.0
libpysal>=4.6.0

# Utilities
networkx>=2.6.0
tqdm>=4.62.0
joblib>=1.1.0
ruamel.yaml>=0.18,<0.19

# Jupyter and visualization (optional, for notebooks)
ipywidgets>=7.6.0
plotly>=5.0.0
ipykernel>=6.0.0

# GPU QP backend (NOT available on PyPI — requires NVIDIA GPU + CUDA)
# Install via NVIDIA package index:
#   pip install cuopt-cu12 --extra-index-url https://pypi.nvidia.com
# or use a pre-built RAPIDS/NGC container.
# There is NO CPU fallback for the proportion solver (Module 3).
# cuOPT is intentionally excluded from this file so that `pip install -r
# requirements.txt` succeeds in CPU-only environments.
