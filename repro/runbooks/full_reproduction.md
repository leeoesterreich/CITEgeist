# Full Reproduction Runbook (v5)

## End-to-end target

```bash
python -m repro.cli.repro --target v5_full --config repro/config/example.paths.yaml
```

This target includes module rerun templates and v5 figure regeneration commands.

## Dry run

```bash
python -m repro.cli.repro --target v5_full --dry-run --config repro/config/example.paths.yaml
```

